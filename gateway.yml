zuul:
  routes:
    id:
      path: /id/**
      serviceId: id
    uaa:
      path: /uaa/**
      serviceId: auth
    messaging:
      path: /messaging/**
      serviceId: messaging
    config:
      path: /config/**
      serviceId: config
