zuul:
  routes:
    id:
      path: /id/**
      serviceId: id
      sensitiveHeaders:
    uaa:
      path: /uaa/**
      serviceId: auth
      sensitiveHeaders:
    messaging:
      path: /messaging/**
      serviceId: messaging
      sensitiveHeaders:
    am:
      path: /am/**
      serviceId: account-management
      sensitiveHeaders:
    config:
      path: /config/**
      serviceId: config
      sensitiveHeaders:
