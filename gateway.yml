zuul:
  routes:
    id:
      path: /id/**
      serviceId: id
      sensitiveHeaders:
    uaa:
      path: /uaa/**
      serviceId: auth
      sensitiveHeaders:
    messaging:
      path: /messaging/**
      serviceId: messaging
      sensitiveHeaders:
    account-management:
      path: /account-management/**
      serviceId: account-management
      strip-prefix: false
      sensitiveHeaders:
    config:
      path: /config/**
      serviceId: config
      sensitiveHeaders:
