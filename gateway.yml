zuul:
  sensitiveHeaders: Cookie, Set-Cookie
  routes:
    id:
      path: /id/**
      serviceId: id
    uaa:
      path: /uaa/**
      serviceId: auth
    messaging:
      path: /messaging/**
      serviceId: messaging
    am:
      path: /am/**
      serviceId: am
    config:
      path: /config/**
      serviceId: config
